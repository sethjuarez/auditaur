---
title: Welcome to Auditaur
description: What Auditaur is and how it helps humans and AI agents debug Tauri apps.
sidebar:
  order: 1
---

import { Aside, Badge } from '@astrojs/starlight/components';

<Aside type="caution" title="MVP in progress">
  Auditaur is early-stage software. The storage layer, CLI read path, discovery, Tauri plugin, frontend package, Rust tracing layer, OpenTelemetry JS span exporter, MCP server, retention pruning, redaction, startup and runtime window lifecycle capture, Tauri-native drive bridge, screenshots/snapshots, agent helper commands, human-in-the-loop drills, and dogfood example are implemented and published. OTLP receiver/sidecar, metrics, browser devtools actions, OpenTelemetry logs SDK support, and automatic OpenTelemetry JS instrumentation setup are still pending.
</Aside>

## What is Auditaur?

**Auditaur** is local-first runtime observability for Tauri apps. A Tauri app opts in during development, emits OpenTelemetry-compatible telemetry, and Auditaur persists that data to SQLite on the developer machine.

The goal is to make Tauri debugging easy for both humans and coding agents: instead of asking for copied logs, browser console output, screenshots, or hand-written summaries, tools can query structured local telemetry.

## MVP scope

| Capability | Status |
| --- | --- |
| SQLite session store | <Badge text="Implemented" variant="success" /> |
| CLI inspection | <Badge text="Implemented" variant="success" /> |
| Tauri plugin | <Badge text="Implemented" variant="success" /> |
| Frontend API package | <Badge text="Implemented" variant="success" /> |
| Rust tracing and third-party span sink | <Badge text="Implemented" variant="success" /> |
| MCP server | <Badge text="Implemented" variant="success" /> |
| App discovery | <Badge text="Implemented" variant="success" /> |
| IPC and event views | <Badge text="Implemented" variant="success" /> |
| Timeline, explain, tail, and bundle CLI workflows | <Badge text="Implemented" variant="success" /> |
| Agent helper commands (`start`, `drill`, `inspect`, `stop`) | <Badge text="Implemented" variant="success" /> |
| Human-in-the-loop drill gates | <Badge text="Implemented" variant="success" /> |
| Window state views | <Badge text="Startup + lifecycle" variant="success" /> |
| Tauri-native drive bridge and screenshots | <Badge text="Implemented" variant="success" /> |
| Rust panic diagnostics | <Badge text="Implemented" variant="success" /> |
| Retention and redaction | <Badge text="Implemented" variant="success" /> |
| OTLP receiver, metrics, browser devtools actions, OTEL logs SDK | <Badge text="Planned" variant="default" /> |

## Where to start

| Goal | Start here |
| --- | --- |
| Try Auditaur in the bundled app | [Quick Start](/getting-started/quick-start/) |
| Add Auditaur to an existing Tauri app | [Integration Happy Path](/getting-started/integration-happy-path/) |
| Give agents a simple startup and drill loop | [Agent Start Here](/getting-started/agent-start-here/) |
| Validate OAuth, OS prompts, and other manual steps | [Human-in-the-Loop Drills](/getting-started/human-in-the-loop-drills/) |
| Understand the moving parts | [Architecture](/concepts/architecture/) |
| Debug a broken setup | [Troubleshooting](/reference/troubleshooting/) |
| Run common command sequences | [CLI Recipes](/reference/cli-recipes/) |

## Security posture

Auditaur is local-only by default, development/debug oriented, and designed to avoid source code, environment variables, secrets, raw request bodies, or arbitrary filesystem collection.
